# OpenAI Images API key, required for --provider openai.
# The default provider is codex when the local codex CLI is installed,
# otherwise wda falls back to openai.
OPENAI_API_KEY=

# Optional: override the image model (default: gpt-image-1)
OPENAI_IMAGE_MODEL=
